from rocket import RocketBoard, Rocket

""" rocket1 = Rocket(height = 5)
rocket2 = Rocket()

print(RocketBoard.get_distance(rocket1, rocket2))
"""
board = RocketBoard(11)
print(len(board))

print(f"Next rocket id will be {Rocket.nextId}")
